<div class="container-formpost">
  <div class="container-fluid ">
    <% if current_user.try(:admin?) %>
      <%= render 'form' %>
      <% else %>
      <%= render 'hiraganas/redirect' %>
    <% end %>
  </div>
</div>
