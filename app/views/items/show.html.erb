<div class="container-page">
  <div class="padding-page">
    <div class="container-fluid">
      <div class="row">
        <%= render 'shared/breadcrumb' %>
        <div class="col-xs-12 col-sm-12 col-md-12">


            <h2><%= @item.title %></h2>

            <div>
              <%= @item.specific.content %>
            </div>

            <%= link_to "< back", [@chapter.course, @chapter] %>
          </div>

      </div>
    </div>
  </div>
</div>

