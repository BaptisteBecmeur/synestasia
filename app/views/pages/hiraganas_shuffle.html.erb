<div class="container-formpost">
  <div class="container">
    <div class="row">

      <div class="text-center">
        <h1>Hiraganas mélangés !</h1>
      </div>

      <ul class="list-inline text-center">
        <% @hiraganas.shuffle.each do |hiragana| %>
          <li>
            <%= render 'hiraganasshuffle', hiragana: hiragana %>
          </li>
          <% end %>
      </ul>

    </div>
  </div>
</div>
