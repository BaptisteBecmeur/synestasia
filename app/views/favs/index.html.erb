<div class="container" id="home-articles">
  <h2>Mes favoris</h2>
  <table class="table table-striped table-hover table-users">
    <thead>
      <tr>
        <th>
          Titre
        </th>
        <th>
          Auteur
        </th>
        <th></th>
      </tr>
    </thead>
    <tbody>
      <% @favs.each do |fav| %>
      <tr>
        <td>
          <%= link_to hiragana_path(fav.hiragana) do %>
          <%= fav.hiragana.ideo1 %>
          <% end %>
        </td>
        <td>
          <%= link_to "Supprimer des favs", fav_path(fav), method: :delete %>
        </td>
      </tr>
      <% end %>
    </tbody>
  </table>

</div>




